Security guidance:

- This scaffold does not enable authentication. Add Spring Security for production-grade auth.
- Do not store secrets in source control. Use environment variables or secret manager.
- Use TLS for all external traffic.
